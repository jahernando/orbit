# Binarios y resultados locales
**/references/
**/results/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/

# macOS
.DS_Store

# Google Calendar credentials (sensitive)
credentials.json
token.json

# Slack / cloud sync state (sensitive or local)
.slack-token
.cloud-sync.json
google-sync.json
calendar-sync.json

# Shell personal (contiene config local)
orbit.sh

# Temp output
cmd.md
.dash-stamp
agenda.md
calendar.md
panel.md
history.md

# Hook system journal (runtime artifact, per-workspace)
.journal.jsonl
.journal.jsonl.1

# Ring desacoplado (runtime artifact, per-workspace)
.reminders/
# Secretary viewers regenerated by run_dash, not tracked
📋secretary/
